<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minecraft Bedrock Servers - Futuristic Hub</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #0f1123 100%);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Animated background */
        .bg-animation {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 0;
            opacity: 0.3;
        }

        .bg-animation::before {
            content: '';
            position: absolute;
            width: 200%;
            height: 200%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(0, 255, 157, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(0, 191, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 40% 20%, rgba(138, 43, 226, 0.1) 0%, transparent 50%);
            animation: bgMove 20s ease-in-out infinite;
        }

        @keyframes bgMove {
            0%, 100% { transform: translate(0, 0); }
            50% { transform: translate(-50px, -50px); }
        }

        .container {
            position: relative;
            z-index: 1;
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header */
        header {
            text-align: center;
            padding: 40px 20px;
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.1) 0%, rgba(0, 191, 255, 0.1) 100%);
            border-radius: 20px;
            margin-bottom: 40px;
            border: 2px solid rgba(0, 255, 157, 0.3);
            box-shadow: 0 0 40px rgba(0, 255, 157, 0.2);
            animation: headerGlow 3s ease-in-out infinite;
        }

        @keyframes headerGlow {
            0%, 100% { box-shadow: 0 0 40px rgba(0, 255, 157, 0.2); }
            50% { box-shadow: 0 0 60px rgba(0, 255, 157, 0.4), 0 0 80px rgba(0, 191, 255, 0.2); }
        }

        h1 {
            font-size: 3.5em;
            background: linear-gradient(135deg, #00ff9d 0%, #00bfff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 10px;
            text-shadow: 0 0 30px rgba(0, 255, 157, 0.5);
            animation: titlePulse 2s ease-in-out infinite;
        }

        @keyframes titlePulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.02); }
        }

        .subtitle {
            font-size: 1.2em;
            color: #00bfff;
            text-shadow: 0 0 10px rgba(0, 191, 255, 0.5);
        }

        /* Stats Bar */
        .stats-bar {
            display: flex;
            justify-content: space-around;
            gap: 20px;
            margin-bottom: 40px;
            flex-wrap: wrap;
        }

        .stat-card {
            flex: 1;
            min-width: 200px;
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.1) 0%, rgba(0, 191, 255, 0.05) 100%);
            border: 2px solid rgba(0, 255, 157, 0.3);
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 255, 157, 0.3);
            border-color: rgba(0, 255, 157, 0.6);
        }

        .stat-number {
            font-size: 2.5em;
            font-weight: bold;
            background: linear-gradient(135deg, #00ff9d 0%, #00bfff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-label {
            color: #8b9dc3;
            margin-top: 5px;
            font-size: 0.9em;
        }

        /* Search Bar */
        .search-container {
            margin-bottom: 30px;
            display: flex;
            justify-content: center;
        }

        .search-box {
            width: 100%;
            max-width: 600px;
            padding: 15px 25px;
            background: rgba(10, 14, 39, 0.8);
            border: 2px solid rgba(0, 255, 157, 0.3);
            border-radius: 50px;
            color: #fff;
            font-size: 1.1em;
            outline: none;
            transition: all 0.3s ease;
        }

        .search-box:focus {
            border-color: rgba(0, 255, 157, 0.6);
            box-shadow: 0 0 20px rgba(0, 255, 157, 0.3);
        }

        .search-box::placeholder {
            color: #8b9dc3;
        }

        /* Server Grid */
        .server-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .server-card {
            background: linear-gradient(135deg, rgba(10, 14, 39, 0.9) 0%, rgba(26, 31, 58, 0.9) 100%);
            border: 2px solid rgba(0, 255, 157, 0.2);
            border-radius: 20px;
            padding: 25px;
            transition: all 0.4s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .server-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 157, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .server-card:hover::before {
            left: 100%;
        }

        .server-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 15px 40px rgba(0, 255, 157, 0.3);
            border-color: rgba(0, 255, 157, 0.6);
        }

        .server-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .server-name {
            font-size: 1.5em;
            font-weight: bold;
            color: #00ff9d;
            text-shadow: 0 0 10px rgba(0, 255, 157, 0.5);
        }

        .server-status {
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.8em;
            font-weight: bold;
            animation: statusPulse 2s ease-in-out infinite;
        }

        .status-online {
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.3) 0%, rgba(0, 255, 157, 0.1) 100%);
            color: #00ff9d;
            border: 1px solid #00ff9d;
        }

        .status-offline {
            background: linear-gradient(135deg, rgba(255, 0, 0, 0.3) 0%, rgba(255, 0, 0, 0.1) 100%);
            color: #ff4444;
            border: 1px solid #ff4444;
        }

        @keyframes statusPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .server-description {
            color: #8b9dc3;
            margin-bottom: 15px;
            line-height: 1.6;
        }

        .server-info {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-bottom: 15px;
        }

        .info-item {
            background: rgba(0, 255, 157, 0.05);
            padding: 10px;
            border-radius: 10px;
            border: 1px solid rgba(0, 255, 157, 0.2);
        }

        .info-label {
            font-size: 0.8em;
            color: #8b9dc3;
            margin-bottom: 3px;
        }

        .info-value {
            font-size: 1.1em;
            color: #00bfff;
            font-weight: bold;
        }

        .server-ip {
            background: linear-gradient(135deg, rgba(0, 191, 255, 0.2) 0%, rgba(0, 255, 157, 0.2) 100%);
            border: 2px solid rgba(0, 191, 255, 0.3);
            border-radius: 10px;
            padding: 12px;
            margin-top: 15px;
            text-align: center;
            font-family: 'Courier New', monospace;
            font-size: 1.1em;
            color: #00bfff;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
        }

        .server-ip:hover {
            background: linear-gradient(135deg, rgba(0, 191, 255, 0.3) 0%, rgba(0, 255, 157, 0.3) 100%);
            box-shadow: 0 0 20px rgba(0, 191, 255, 0.4);
        }

        .copy-btn {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(0, 255, 157, 0.2);
            border: 1px solid rgba(0, 255, 157, 0.4);
            color: #00ff9d;
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 0.8em;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .copy-btn:hover {
            background: rgba(0, 255, 157, 0.4);
        }

        .server-tags {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin-top: 15px;
        }

        .tag {
            background: linear-gradient(135deg, rgba(138, 43, 226, 0.3) 0%, rgba(75, 0, 130, 0.3) 100%);
            border: 1px solid rgba(138, 43, 226, 0.5);
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.85em;
            color: #ba55d3;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 30px;
            background: linear-gradient(135deg, rgba(0, 255, 157, 0.05) 0%, rgba(0, 191, 255, 0.05) 100%);
            border-radius: 20px;
            border: 2px solid rgba(0, 255, 157, 0.2);
            margin-top: 40px;
        }

        footer p {
            color: #8b9dc3;
            margin: 5px 0;
        }

        .footer-links {
            margin-top: 15px;
        }

        .footer-links a {
            color: #00bfff;
            text-decoration: none;
            margin: 0 15px;
            transition: all 0.3s ease;
        }

        .footer-links a:hover {
            color: #00ff9d;
            text-shadow: 0 0 10px rgba(0, 255, 157, 0.5);
        }

        /* Responsive */
        @media (max-width: 768px) {
            h1 {
                font-size: 2em;
            }

            .server-grid {
                grid-template-columns: 1fr;
            }

            .stats-bar {
                flex-direction: column;
            }
        }

        /* Loading Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .server-card {
            animation: fadeIn 0.5s ease-out;
        }
    </style>
</head>
<body>
    <div class="bg-animation"></div>
    
    <div class="container">
        <header>
            <h1>⛏️ MINECRAFT BEDROCK</h1>
            <p class="subtitle">Premium Server List - Futuristic Edition
